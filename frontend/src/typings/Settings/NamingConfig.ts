export default interface NamingConfig {
  renameEpisodes: boolean;
  replaceIllegalCharacters: boolean;
  colonReplacementFormat: number;
  standardEpisodeFormat: string;
  javEpisodeFormat: string;
  seriesFolderFormat: string;
}
